- if line_item == @current_item
  %tr#current_item.current_item
- else
  %tr.current_item
%td
  = line_item.quantity
  \×
%td= line_item.product.title
%td.item_price= number_to_currency(line_item.total_price)
%td=button_to 'Delete', line_item_path(line_item), :method => :delete, :confirm => 'Are you sure?', :remote => true